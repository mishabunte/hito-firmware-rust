import { Router } from "Router.slint";

export component TestScreen2View {
    width: 320px;
    height: 240px;
    Text {
        text: "Test screen !)";
        font-size: 20px;
        color: black;
        horizontal-alignment: center;
        y: 120px;
        x: 30px;
        wrap: word-wrap;
        width: 260px;
    }
}