import { Palette } from "Palette.slint";

export component TransparentButton {
    in-out property <bool> pressed: false;
    in-out property <string> content: "0";
    in-out property <length> in-font-size: 20px;
    in-out property <bool> border-shown: true;
    in-out property <color> text-color-unpressed: Palette.blue;
    in-out property <color> text-color-pressed: Palette.black;
    property <length> touch-padding: 10px;
    Text {
        text: content;
        font-size: root.in-font-size;
        color: pressed ? text-color-pressed : text-color-unpressed;
        horizontal-alignment: center;
        vertical-alignment: center;
        TouchArea {
            x: parent.x - touch-padding;
            y: parent.y - touch-padding;
            width: parent.width + 2*touch-padding;
            height: parent.height + 2*touch-padding;
            pointer-event(event) => {
                if (event.kind == PointerEventKind.down) {
                    pressed = true;
                } else if (event.kind == PointerEventKind.up) {
                    pressed = false;
                    root.clicked();
                }
            }
        }
    }
    callback clicked();
}