import { SendScreen } from "SendScreen.slint";

export component PairWithTheAppScreen inherits SendScreen {
    header-title: "PAIR WITH THE APP";
    // Additional UI elements for the Pair with the App screen can be added here
}