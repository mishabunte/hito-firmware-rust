import { SendScreenView } from "SendScreen.slint";

export component PairWithTheAppScreenView inherits SendScreenView {
    // header-title: "PAIR WITH THE APP";
    // // Additional UI elements for the Pair with the App screen can be added here
}